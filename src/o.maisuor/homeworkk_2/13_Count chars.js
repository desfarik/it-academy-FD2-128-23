function consonantCount(str) {
    const counter = 0
    let vowels = ['a', 'e', 'i', 'o', 'u']
    if (str.includes(vowels)){
      counter++
    }
  return counter;
      let word = str.toLowerCase()
  //     if(/[aeiouy]/.test(word)){
  //    return counter++
  // } 
  // else {
  //    return 0
  // }
 
}

console.log(consonantCount(abcdefghijkopru))